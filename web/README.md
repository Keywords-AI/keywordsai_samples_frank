# web — setup.respan.ai (v1)

Next.js dashboard: Respan sign-in, GitHub connect + repo picker, the **questionnaire**
(pre-filled by a repo pre-scan), credits/BYOK for gateway, and the **live progress UI**
(cloning → detected libs → file diffs → PR link → first trace).

Progress streams from the session runner over SSE. Deploy on Railway.
